<link rel="stylesheet" href="/css/nav_bar.css">
<link rel="stylesheet" href="/css/userpage.css">
<link rel="stylesheet" href="/css/notice_number.css">
<link rel="stylesheet" href="/css/notice_detail.css">
<link rel="stylesheet" href="/css/function.css">
<link rel="stylesheet" href="/css/article.css">
<link rel="stylesheet" href="/css/subscription.css">
<link rel="stylesheet"
    href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />


<body>

    {{> nav_bar_login}}

    {{#if notification}}
    {{> notice_detail}}
    {{/if}}

    <div class="main">

        {{> function}}

        <div class="container">
            <div class="subscription_container">
                <div class="left">
                    <h2>List of Following</h2>
                    {{#if authors}}
                    <ul>
                        {{#each authors}}
                        <li class="author_card">
                            <a href="./subscription/author?name={{this.username}}">
                                <img src="{{this.avatar}}" width="40">
                                <div class="author_detail">
                                    <span>{{this.username}}</span>
                                    <div class="author_profile">
                                        Profile: {{this.profile}}
                                    </div>
                                </div>
                            </a>
                        </li>
                        {{/each}}
                    </ul>
                    {{else}}
                    <p>No existing authors</p>
                    {{/if}}
                </div>
                <div class="right">
                    <h2>List of Followers</h2>
                    {{#if subscribers}}
                    <ul>
                        {{#each subscribers}}
                        <li class="subscriber_card">
                            <a href="./subscription/subscriber?name={{this.username}}">
                                <img src="{{this.avatar}}" width="40">
                                <div class="subscriber_detail">
                                    <span>{{this.username}}</span>
                                    <div class="subscriber_profile">
                                        Profile: {{this.profile}}
                                    </div>
                                </div>
                            </a>
                        </li>
                        {{/each}}
                    </ul>
                    {{else}}
                    <p>No existing followers</p>
                    {{/if}}
                </div>
            </div>
        </div>
</body>